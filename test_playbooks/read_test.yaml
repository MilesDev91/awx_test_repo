---
- name: Read-only 834
  hosts: localhost
  gather_facts: false
  tasks:
    - name: List collections in this EE
      ansible.builtin.command: ansible-galaxy collection list
      register: out

    - debug:
        var: out.stdout_lines
